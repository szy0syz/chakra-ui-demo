import {
	Image,
	Box,
	Tabs,
	TabList,
	Tab,
	TabPanels,
	TabPanel,
	useColorMode,
} from '@chakra-ui/core';

const form = () => {
	const { colorMode } = useColorMode();
	return (
		<div></div>
	);
};

export default form;
